# Keep secrets and workspace-only files out of the publish upload when
# publish = "." is used.
.env
.env.*
.git/
.agents/
.codex/
.claude/
tools/
.github/
scripts/
node_modules/
*.log
*.zip
sitemap.zip

# Internal docs, plans and backups — never publish (publish=".")
*.md
*.bak*
docs/

# Repo / backend scaffolding — not part of the public site
supabase/
package.json
package-lock.json
propostas/
